# Data Volumes Example

This project contains the Docker data volumes example app used in this repository.

## Notes

- The app directory now lives under `volumes/data-volumes/`.
- Feedback files are written to the `feedback/` directory.
- The temporary working files are created in the `temp/` directory.
